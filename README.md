Вот обновленный `README.md`, который соответствует новой архитектуре, функционалу и структуре папок вашего проекта.

--- START OF FILE README.md ---

# JavaAnki: Master Edition

**JavaAnki** — это продвинутое десктопное приложение для интервального повторения (Spaced Repetition System), написанное на чистой Java (Swing). Проект прошел полный рефакторинг с использованием слоистой архитектуры (Layered Architecture), став модульным, расширяемым и стабильным.

## ✨ Основные возможности

*   **Умный алгоритм обучения:** Карточки имеют уровни (0-10). Чем лучше вы знаете ответ, тем реже появляется вопрос. Используется взвешенный рандом.
*   **Fuzzy Matching:** Проверка ответов устойчива к опечаткам (алгоритм Левенштейна). Система показывает процент сходства.
*   **Глубокая темизация:** Полная поддержка Темной и Светлой тем. Кастомные скроллбары, заголовки таблиц, выпадающие списки и диалоговые окна.
*   **Импорт данных:** Умный импорт из файла `import.txt` с автоматическим удалением дубликатов и распределением по файлам.
*   **Группировка тем:** Возможность объединять несколько файлов колод в одну логическую группу (например, "Java Core").
*   **Статистика и История:** Подробные логи обучения и таблица прогресса.
*   **Горячие клавиши:** Полное управление с клавиатуры для высокой скорости повторения.

---

## 🛠 Технологический стек

*   **Язык:** Java 17+
*   **UI:** Swing (полностью кастомная отрисовка компонентов, без использования сторонних LaF библиотек).
*   **Архитектура:** Layered Architecture (Repository -> Service -> UI).
*   **Работа с данными:** Java NIO.2 (Files, Path).
*   **Инструменты:** Lombok, Java Util Logging.

---

## 🚀 Установка и Запуск

### Требования
1.  **JDK 17** или выше.
2.  Библиотека **Lombok** (`lombok.jar`).

### Запуск из IDE (IntelliJ IDEA, Eclipse)
1.  Убедитесь, что плагин Lombok установлен и включен в IDE.
2.  Откройте проект.
3.  Запустите класс `app.App`.

### Запуск через консоль
Так как используется Lombok, его нужно подключить в classpath при компиляции:

```bash
# Компиляция (укажите путь к вашему lombok.jar)
javac -cp "libs/lombok.jar;src" -d out src/app/App.java src/*/*.java src/*/*/*.java

# Запуск
java -cp "libs/lombok.jar;out" app.App
```

---

## 📖 Руководство пользователя

### 1. Добавление вопросов (Импорт)
Чтобы добавить новые карточки, создайте файл `import.txt` в корневой папке программы.
Формат записи:

```text
CATEGORY: Java Collections
QUESTION:
В чем разница между ArrayList и LinkedList?
ANSWER:
ArrayList на массиве (быстрый доступ), LinkedList на связном списке (быстрая вставка).
===
CATEGORY: SQL
QUESTION:
Что такое JOIN?
ANSWER:
Операция объединения таблиц.
===
```

**Как это работает:**
1.  Перейдите на вкладку **"Статистика"**.
2.  Нажмите **"Импорт из txt"**.
3.  Система проверит вопросы на дубликаты (сравнивая с базой на диске).
4.  Успешные вопросы будут добавлены в файлы `decks/НазваниеТемы.txt` и удалены из `import.txt`.
5.  Ошибочные блоки останутся в `import.txt` для исправления.

### 2. Структура групп (`decks/structure.txt`)
Вы можете объединять файлы колод в группы. Создайте файл `decks/structure.txt`:

```text
GROUP: Java Core
FILES: oop.txt, collections.txt, exceptions.txt

GROUP: Databases
FILES: sql_basics.txt, jdbc.txt
```
Теперь в выпадающем списке выбора тем появятся "Java Core" и "Databases".

### 3. Горячие клавиши (Вкладка Обучение)
*   **ENTER** — Показать ответ / Проверить ввод.
*   **→ (Стрелка ВПРАВО)** — Отметить как "Верно".
*   **← (Стрелка ВЛЕВО)** — Отметить как "Ошибка".

---

## 📂 Структура данных

Программа создает и использует следующие файлы:

*   `decks/` — Папка с текстовыми файлами вопросов (можно редактировать вручную).
*   `decks/structure.txt` — Настройка групп тем.
*   `anki_stats.txt` — Хранилище прогресса (хэш вопроса | уровень).
*   `history_log.txt` — Журнал всех ответов.
*   `import.txt` — Временный файл для добавления новых карт.

---

## 🔧 Структура проекта (для разработчиков)

```text
src/
├── app/            # Точка входа (App.java)
├── config/         # Цветовые палитры и константы
├── data/           # Низкоуровневая работа с файлами
├── data/repository # Логика чтения конкретных форматов (Decks, History, Stats)
├── model/          # POJO классы (Card, HistoryRecord)
├── service/        # Бизнес-логика (StudyService, ImportService, Grading)
├── ui/             # Графический интерфейс
├── ui/components   # Кастомные кнопки, скроллы, заголовок окна
├── ui/panels       # Панели вкладок (Study, Stats, History)
└── util/           # Утилиты (нормализация текста)
```

---

## 🐞 Решение проблем

**1. Статистика показывает 0 вопросов, хотя файлы есть.**
Убедитесь, что кодировка файлов — **UTF-8**. Если вы правили файлы вручную, проверьте, что разделитель `===` находится на отдельной строке.

**2. Я удалил файл вопроса, но он все еще появляется.**
Нажмите кнопку **"Обновить"** на вкладке Статистика или перезапустите программу. Сервис кэширует данные для производительности, но принудительно обновляет их при импорте или ручном обновлении.

**3. Дубликаты при импорте.**
Смотрите консоль/логи IDE. Программа выводит предупреждения вида `WARNING: ДУБЛИКАТ: Файл [...] vs [...]`, если вы пытаетесь добавить вопрос, который уже существует (даже в другом файле).